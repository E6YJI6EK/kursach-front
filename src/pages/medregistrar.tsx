import React, { FC } from 'react';
import s from './Medregistrar.module.scss';

interface MedregistrarProps {
  className?: string;
}

export const MedregistrarPage: FC<MedregistrarProps> = (props) => {
  const { className } = props;

  return (
    <div>

    </div>
  );
};